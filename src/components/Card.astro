---
const{first, last, gender, email, phone, picture} = Astro.props
---

<article class="flex flex-col items-center px-5 py-5 w-80 h-80 gap-2 rounded-2xl shadow-lg border-1 border-gray-300">
    <img class="rounded-full shadow-lg" src={picture} alt="">
    <div class="text-center flex flex-col items-center gap-1">
        <h2 class="font-bold">{first} {last}</h2>
        <p class="italic">{gender}</p>
        <p class="">{email}</p>
        <p class="bg-transparent w-fit rounded-full text-center px-2 py-1 mt-2 text-black border-2 font-bold">{phone}</p>
    </div>

</article>